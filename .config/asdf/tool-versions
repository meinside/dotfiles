# .config/asdf/tool-versions
#
# https://github.com/asdf-vm/asdf-plugins
#
# created on : 2022.04.14.
# last update: 2025.11.17.

# $ asdf plugin add babashka
# $ asdf install babashka
#
# https://github.com/babashka/babashka/releases
babashka 1.12.209

# For bash-language-server,
#   :MasonInstall bash-language-server

# $ asdf plugin add clojure https://github.com/asdf-community/asdf-clojure.git
# $ asdf install clojure
#
# For language server,
#   :MasonInstall clojure-lsp
#
# https://clojure.org/releases/tools#_releases
clojure 1.12.3.1577

# $ asdf plugin add elixir
# $ asdf install elixir
#
# * NOTE: `erlang` needs to be installed.
#
# https://github.com/elixir-lang/elixir/releases
elixir 1.19.0

# $ asdf plugin add erlang
# $ KERL_BUILD_DOCS=yes asdf install erlang
#
# https://github.com/erlang/otp/releases
erlang 28.1.1

# $ asdf plugin add gleam
#
# https://github.com/gleam-lang/gleam/releases
gleam 1.13.0

# $ asdf plugin add golang
# $ asdf install golang
#
# reshim is needed after installing things with `go install`:
#   $ asdf reshim golang
#
# link or copy binaries to $GOPATH/bin/ if needed:
#   $ cp `asdf which XXXX` $GOPATH/bin/
#
# https://go.dev/doc/devel/release
golang 1.25.4

# $ asdf plugin add janet
# $ asdf install janet
#
# If meson is not installed,
#   $ asdf plugin add meson && \
#     asdf install meson && \
#     asdf plugin add ninja && \
#     asdf install ninja
# or
#   $ brew install meson
# or
#   $ sudo apt install meson
# or
#   $ pip3 install --user meson
#
# For language server,
#   $ jpm install spork
#   $ janet -e "(import spork/netrepl) (netrepl/server)"
#
# * NOTE: if an error like "cannot open shared object file: No such file or directory" occurs,
#   append following line in the shell rc file:
#   `export LD_LIBRARY_PATH=$HOME/.asdf/installs/janet/X.XX.XX/lib`
#
# https://github.com/janet-lang/janet/releases
janet 1.40.1

# $ asdf plugin add java
# $ asdf install java
#
# https://github.com/halcyon/asdf-java/tree/master/data
java openjdk-24.0.2

# $ asdf plugin add leiningen
# $ asdf install leiningen
#
# https://github.com/technomancy/leiningen/releases
leiningen 2.12.0

# $ asdf plugin add lua
# $ asdf install lua
#
# For language server,
#   :MasonInstall lua-language-server
#
# For readline in REPL,
#   $ sudo apt install liblua5.4-dev libreadline-dev
#   $ luarocks --local install readline HISTORY_LIBDIR=/usr/lib/aarch64-linux-gnu HISTORY_INCDIR=/usr/include READLINE_LIBDIR=/usr/lib/aarch64-linux-gnu
# or
#   $ brew install readline
#   $ luarocks --local install readline HISTORY_LIBDIR=/opt/homebrew/opt/readline/lib HISTORY_INCDIR=/opt/homebrew/opt/readline/include READLINE_LIBDIR=/opt/homebrew/opt/readline/lib
#
# and put `luarocks path` result to .custom_env file
#
# https://www.lua.org/versions.html
lua 5.1.5 5.4.8

# $ asdf plugin add meson
# $ asdf install meson
#
# https://github.com/mesonbuild/meson/releases
meson 1.9.1

# $ asdf plugin add nim
# $ asdf install nim
#
# For language server,
#   :MasonInstall nimlsp
#
# https://nim-lang.org/install.html
nim 2.2.6

# $ asdf plugin add ninja
# $ asdf install ninja
#
# https://github.com/ninja-build/ninja/releases
ninja 1.13.1

# $ asdf plugin add nodejs
# $ asdf install nodejs
#
# https://nodejs.org/en/about/releases/
nodejs 25.2.0

# $ asdf plugin add python
# $ asdf install python
#
# For language server,
#   :MasonInstall python-lsp-server
#
# https://www.python.org/downloads/
python 3.14.0

# $ asdf plugin add ruby
# $ asdf install ruby
#
# NOTE: if the latest version is not available yet,
# ASDF_RUBY_BUILD_VERSION=master asdf install ruby x.y.z
#
# if installation fails with psych on macOS,
# $ brew install libyaml
#
# For language server,
#   :MasonInstall solargraph
#
# https://www.ruby-lang.org/en/downloads/releases/
ruby 3.4.7

# $ asdf plugin add rust
# $ asdf install rust
#
# For rust-analyzer, (https://rust-analyzer.github.io/manual.html#installation)
#   :MasonInstall rust-analyzer
#
# https://github.com/rust-lang/rust/releases
rust 1.91.1

# $ asdf plugin add tinygo https://github.com/troyjfarrell/asdf-tinygo
# $ asdf install tinygo
#
# https://github.com/tinygo-org/tinygo/releases/
tinygo 0.39.0

# $ asdf plugin add zig
# $ asdf install zig
#
# For language server,
#   :MasonInstall zls
#
# https://github.com/ziglang/zig/releases
zig 0.15.2

