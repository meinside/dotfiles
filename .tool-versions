# My .tool-versions for `asdf`
#
# created on : 2022.04.14.
# last update: 2022.07.15.

# $ asdf plugin add babashka
# $ asdf install babashka
#
# https://github.com/babashka/babashka/releases
babashka 0.8.157

# For bash-language-server,
#   $ npm i -g bash-language-server

# For ccls,
#   $ brew install ccls
# or
#   $ sudo apt install ccls

# $ asdf plugin add clojure https://github.com/asdf-community/asdf-clojure.git
# $ asdf install clojure
#
# For clojure-lsp,
#   $ brew install clojure-lsp/brew/clojure-lsp-native
# or
#   $ sudo bash < <(curl -s https://raw.githubusercontent.com/clojure-lsp/clojure-lsp/master/install)
#
# https://clojure.org/releases/tools#_releases
clojure 1.11.1.1149

# $ asdf plugin add golang
# $ asdf install golang
#
# For gopls,
#
#   $ go install golang.org/x/tools/gopls@latest
#
# https://go.dev/doc/devel/release
golang 1.18.4

# $ asdf plugin add janet
# $ asdf install janet
#
# For nrepl,
#
# $ jpm install spork
# $ janet -e "(import spork/netrepl) (netrepl/server)"
#
# https://github.com/janet-lang/janet/releases
janet 1.23.0

# $ asdf plugin add java
# $ asdf install java
java openjdk-17.0.2

# $ asdf plugin add leiningen
# $ asdf install leiningen
#
# https://github.com/technomancy/leiningen/releases
leiningen 2.9.8

# $ asdf plugin add lua
# $ asdf install lua
#
# For lua-language-server,
#
#   $ brew install lua-language-server
# or
#   $ DIR="/opt/lua-language-server"
#   $ sudo mkdir -p $DIR && sudo chown -R $USER $DIR && git clone https://github.com/sumneko/lua-language-server $DIR
#   $ cd $DIR && git submodule update --init --recursive
#   $ cd 3rd/luamake && ./compile/install.sh && cd ../../ && ./3rd/luamake/luamake rebuild
#
# For readline in REPL,
#
#   $ sudo apt install liblua5.4-dev libreadline-dev
#   $ luarocks --local install readline HISTORY_LIBDIR=/usr/lib/aarch64-linux-gnu HISTORY_INCDIR=/usr/include READLINE_LIBDIR=/usr/lib/aarch64-linux-gnu
#
#   # and put `luarocks path` result to .custom_env file
#
# https://www.lua.org/versions.html
lua 5.4.4

# $ asdf plugin add nodejs
# $ asdf install nodejs
#
# https://nodejs.org/en/about/releases/
nodejs lts

# $ asdf plugin add python
# $ asdf install python
#
# For language server,
#   $ pip install --user python-lsp-server
#
# https://www.python.org/downloads/
python 3.10.5

# $ asdf plugin add ruby
# $ asdf install ruby
#
# For language server,
#   $ gem install --user-install solargraph
#
# https://www.ruby-lang.org/en/downloads/releases/
ruby 3.1.2

# $ asdf plugin add rust
# $ asdf install rust
#
# For rust-analyzer, (https://rust-analyzer.github.io/manual.html#installation)
#   $ git clone https://github.com/rust-analyzer/rust-analyzer.git
#   $ cd rust-analyzer/
#   $ cargo xtask install --server
#
# https://github.com/rust-lang/rust/releases
rust 1.62.0

# $ asdf plugin add zig
# $ asdf install zig
#
# For zls, (https://github.com/zigtools/zls)
#   $ sudo rm -rf /opt/zls
#   $ sudo mkdir -p /opt/zls
#   $ sudo chown -R "$USER" /opt/zls
#   $ git clone --recurse-submodules https://github.com/zigtools/zls /opt/zls
#   $ cd /opt/zls
#   $ git checkout master
#   $ zig build -Drelease-safe
#   $ zig-out/bin/zls config
#
# https://github.com/ziglang/zig/releases
zig 0.9.1

